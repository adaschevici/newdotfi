To enable the themes and everything you need to copy the kitty files into the kitty config directory. You can do this by running the following command:

```bash
cp -r <REPO_PATH>/kitty/* ~/.config/kitty/
```
